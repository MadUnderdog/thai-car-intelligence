# P98 cycle report — self-improvement cleanup + Phase 1 route/spec planning

- generated 2026-09-25 (cycle `2026-09-25-1`) · branch `fix/p1-provenance-gate`
- coverage unchanged: **18/32 (56.2%)**, blocked 14 · **0 newly covered brands this cycle**
- scope honoured: no Phase 3, no DB promotion, no AI reconciliation, no verifier change,
  no model/provider/config change, PR #3 left OPEN and UNMERGED

## 1. Self-improvement cleanup (performed before any acquisition)

The P97 run log reported `Self-improvement review: Skill 'source-backed-data-ingestion'
patched · … (references/evidence-locator-integrity.md)`. The curator ledger attributes four
mutations to session `20260925_141123_c931fc36` at `2026-09-25T08:53:02Z` / `08:53:22Z`
(15:53 +07) — i.e. inside that run. No assistant tool call in that session touched a skill
file (its 123 tool calls were terminal/execute_code/patch/write_file against the project),
so the writes came from the automatic background self-improvement review, not from the task.

| file | pre-run sha256 (restored) | post-run sha256 (removed) |
|---|---|---|
| `~/.hermes/skills/data-engineering/source-backed-data-ingestion/SKILL.md` | `78825f53218d3fe047d9c4dd5bb57e2f4354a300a1ba3c062ddec22153fab810` | `9407eb3c00b144584548f177c86e0e59bc3cf7c0674adf967a2dec1aa684518a` |
| `…/references/evidence-locator-integrity.md` | `aa4869e59d6762ecb6209ce03024d0c65c7ec98aa81bc76092f0163dbb8ea221` | `06d0bfec13eba4736fe0146aeecdb49145ba68530c21f70c48c26744bffa35af` |

Restored byte-for-byte from the content-addressed blobs in
`~/.hermes/.curator_backups/blobs/<sha256>`; both files now hash exactly to their
pre-run values (verified with `sha256sum`). Removed content was limited to those two
patches: 5 added lines in `SKILL.md` (1 test rule + 4 session lessons) and 2 added
sections in the reference file. No other skill/reference/memory file changed during the
run window (`2026-09-25T07:11Z–09:01Z`), and no project source code was touched by the cleanup.

## 2. Repository state

- `16316c4fead503e1f8a65ea12eb5224582ebd65c` == `origin/fix/p1-provenance-gate`, tree clean
  at the start of this cycle; `audit/coverage/oem-registry.json` **not modified**.

## 3. SMART registry URL target — verified, not corrected

`smart.co.th` redirects to `smartsecurity.in.th` (a security company). Evidence gathered for
a corrected target found **none**, so the registry URL was left unchanged:

- `https://www.smart.com/th/en|/th/th|/en_th|/th_th` → 302 → `https://global.smart.com/`
  (200, 4837 B, `ssl_verify_result=0`): country chooser, **0 THB markers**
- the chooser's server-rendered markets are at/be/de/ch/es/fr/uk/it/nl/pt/se/fi + smart.com —
  **no Thailand entry**
- official smart newsroom JSON-LD: *smart and PROTON Sign General Distributer Agreement for
  Malaysia and Thailand* (datePublished 2022-08-18) — distributor named, **no Thai site published**
- substitute domains: `smartthailand.co.th`, `smart-thailand.com`, `smartautomobile.co.th`,
  `thonburigroup.com` → NXDOMAIN; `smartthailand.com` → 200 but a 114-byte empty page

Captured as decision evidence (artifact + sidecar each, both `ACQUISITION_VERIFIED`):
`audit/coverage/smart-url-decision-20260925/smart_com_th_en.html` (`e9c40c01…`) and
`…/smart_newsroom_proton_thailand.html` (`f6947083…`). Both are marked
*URL-decision evidence only; not a brand acquisition endpoint, never a source of business rows*.

Consequence: the `2026-09-25T14:16:16Z` (21:16 +07) retry re-probes `https://www.smart.co.th/`
unchanged.

## 4. Alternate official routes for the 14 blocked brands

Artifact: `audit/coverage/alternate_official_routes_20260925.json`. No registry-blocked host
was requested this cycle; all checks used default certificate validation; DNS-dead hosts were
looked up only.

Prepared (verified or ladder-ready):

- **Volvo** — `https://www.volvocars.com/th/` and `/en-th/` (host differs from the TLS-broken
  `www.volvo.co.th`, certificate-valid): non-browser GET returns HTTP 403 "Access Denied";
  browser-level attempt deferred to `2026-10-24`.
- **Ford** — official price sheet `https://www.ford.co.th/en/buying/price-list/` (ladder rung
  for `2026-10-01`, host currently 403 — not requested now).
- **Mercedes-Benz** — official all-models index and configurator on `mercedes-benz.co.th`
  (`2026-10-01`, not requested now).
- **Chevrolet** — `chevrolet.co.th` probe was 200 but acquisition was 403 via Playwright;
  browser-level retry `2026-10-01`.
- **Haval** — `gwm.co.th/th/models/haval-h6` already captured (zero fetch, sidecar-verified);
  **not** staged as Haval rows: the page is published by GWM Thailand and names the model, not
  the brand — sibling-brand rule requires the page itself to prove the brand.
- **NETA** — `netaauto.com` resolves but the TLS connection times out (30 s) from this network.
- **BYD** — host + certificate healthy, blocker is path-level (`/th/` 404); path/sitemap
  discovery deferred to `2026-10-08`.

Rejected / still open (documented with reasons):

- **Avance** — `avancethailand.com` is AVANCE(Thailand) Co.,Ltd., a jewellery/creative company:
  name collision, never register it.
- **Subaru, Chery** — apex + www (+ `cherythailand.*`) NXDOMAIN; alternate-host discovery open
  (search engines rate-limited this cycle).
- **Tesla, Audi, Peugeot** — hosts resolve; block is edge-level 403; retry only.
- No route was found that can be captured today, which is why coverage did not move.

## 5. Spec coverage planning (plan only — nothing staged, nothing harvested)

Plan regenerated by `scripts/generate_spec_coverage_plan.py` → `audit/spec-coverage-plan.json`:

- current state (recomputed from staging): **72/324 = 22.2%**, brands `['porsche']`, **0 new rows**
- highest-confidence official sources already in hand (zero fetch), ranked by markers:
  1. `lexus_models_page.html` — 312 markers, **structured_field / high** (per-model embedded JSON:
     `spec1key`/`spec1value`, `maxout`, `maxtorque`, `displacement`, `fuelconsumption`)
  2. `mazda_home_page.html` — 105 markers, **spec_block_dom / high** (per-model spec blocks)
  3. `porsche_macan_model_page.html` — 83 markers, high (already the only staged spec brand)
- planning/validation code improved (no production DB write, no harvesting):
  - each tier-1 entry now carries `structure` + `confidence` derived from the artifact bytes
  - new `route_safety` section flags every registry-blocked/wrong-target host with its
    `next_retry_at`, so no spec cycle plans a fetch against a blocked endpoint
  - wrong-target captures (Smart's `smart_home_page.html`) are excluded from every tier
  - `current_state` and all new assertions recompute; the previously hardcoded `72` / `['porsche']`
    literals in the test were replaced by a staging recompute
  - generation proven offline (socket blocked) and byte-reproducible
- `RESEARCH_UNVERIFIED` media rows were not promoted.

## 6. Tests and security

- `pytest -q --continue-on-collection-errors`: **376 passed, 1 error** in 360.77 s
  (baseline 372 + 4 new spec-plan tests; the single error is the **pre-existing** collection
  ImportError in `tests/test_generation_config_boundary.py` for `_OPENROUTER_ALLOWED_MODEL` —
  neither that test nor `lib/thai_factory/extract/ai_extractor.py` is in this diff)
- `npx prisma validate` → rc=0 (pre-existing schema warning only)
- `npx tsc --noEmit` → rc=0
- credential scan over changed/new files (`git status --porcelain -uall`): **9 files, 0 hits**

## 7. Not touched

production database · AI model/provider/config · verifier (FROZEN) · PR #3 merge state ·
blocked hosts before their `next_retry_at` · skills/references/memory beyond the cleanup in §1
